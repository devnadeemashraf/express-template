describe("should login a user if the credentials are correct", function () {});

describe("should throw error when incorrect credentials are entered", function () {});

describe("should throw error when user is not found", function () {});

describe("should throw error if username is less than MIN CHAR Count", function () {});

describe("should throw error if password is less than MIN CHAR Count", function () {});

describe("should throw error if more than MAX ALLOWED sessions are already active for a user", function () {});
