describe("should register a user with username and password", function () {});

describe("should throw error when user with similar username is found", function () {});

describe("should throw error if username is less than MIN CHAR Count", function () {});

describe("should throw error if password is less than MIN CHAR Count", function () {});

describe("should throw error if password contains username", function () {});

// Add Support for Stronger Password Checking in Future
